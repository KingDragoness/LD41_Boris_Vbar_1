using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OilScript : MonoBehaviour {

	private void OnTriggerEnter(Collider other) {
		//if (other.CompareTag ("food")) {
		//	FoodObject fo = other.GetComponent<FoodObject> ();
		//	if (fo.itemID == 8) {
		//		Instantiate (_consoleLD41.mainConsole.prefabExplosion, transform.position, transform.rotation);
		//	}
		//} else if (other.CompareTag ("molotov")) {

	}
}
